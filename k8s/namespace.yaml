apiVersion: v1
kind: Namespace
metadata:
  name: medtext
  labels:
    name: medtext
    environment: production
    app: medical-text-classification
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: medtext-quota
  namespace: medtext
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    persistentvolumeclaims: "10"
    services: "10"
    secrets: "20"
    configmaps: "20"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: medtext-limits
  namespace: medtext
spec:
  limits:
  - default:
      cpu: "1"
      memory: "2Gi"
    defaultRequest:
      cpu: "100m"
      memory: "256Mi"
    type: Container
